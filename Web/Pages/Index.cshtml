@page
@model IndexModel
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@{
    ViewData["Title"] = "Home page";
    var user = await UserManager.GetUserAsync(User);
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>


    @* <button asp-for="@user.Id" type="submit" class="btn btn-primary" onclick="CreateSchedule()" value="@user.Id"> Create Schedule</button> *@

    <form method="post">
        <input type="hidden" name="userId" value="@user.Id" />
        <button type="submit" class="btn btn-primary">Create Schedule</button>
    </form>


</div>


@* 
<div class="row">
    <div class="col-md-4">
        <form method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <p>Workout Name</p>
                <input asp-for="workout.Name" class="form-control" placeholder="Enter workoutname" />
                <button type="submit" class="btn btn-primary">Create</button>
                <span asp-validation-for="workout.Name" class="text-danger"></span>

            </div>
            <div class="form-group">
                <input asp-for="workout.UserId" value="@user.Id" readonly class="form-control" type="hidden" />
                <span asp-validation-for="workout.UserId" class="text-danger"></span>
            </div>



        </form>
    </div>
</div>
 *@